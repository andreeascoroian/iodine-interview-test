//Download NodeJs -> https://nodejs.org/en/download/
//1. Install Node + npm and run following commands in terminal:
//- node -v
//- npm -v

//2. Run tests:
//First time installation of cypress
//In order to open and run the project you must do the following:
//- npm install cypress --save-dev
//- npm install

//to run tests in browser run following command: npx cypress open - choose browser - click on "IodineTest.cy.js"
//to run tests in headless mode run the following command: npx cypress run
